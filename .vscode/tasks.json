{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "npx tsc --watch",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}